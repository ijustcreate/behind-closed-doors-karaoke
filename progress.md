Original prompt: Start Encore Royale with cute programmatic rectangle fighters, a full fluid animation suite, TowerFall-like movement and music-note arrow combat using songs users have sung, battle-royale action with infinite respawns and random spawn points, plus an admin-account button to enter it. The visual retro controller should only appear in the installed app.

- Replaced the early monolithic starter with an isolated Vite/TypeScript game project under `encore-royale/`, with engine, bridge, game systems, rendering, UI, tests, and architecture docs.
- Implemented idle, run, jump, fall, land, crouch, wall-slide, dodge, shoot, KO, and respawn visual states.
- Implemented keyboard/touch input, wall jumps, directional shots, dodge invulnerability, two-note ammo, recoverable arrows, song-name projectile labels, bot opponents, kills, feed, and infinite respawns.
- Added `render_game_to_text` and deterministic `advanceTime(ms)` hooks for browser testing.
- Wired the installed-only parent launcher into `index.html` and service-worker cache v29. The game is preloaded in an iframe under a resistant bottom-of-songbook pull, BCD logo clue, and opening curtain mask. An installed-admin profile button bypasses the discovery journey for testing.
- Added the character lobby: live bots remain active in a blurred backstage arena, while the local player is sharp on a private foreground apron. Players choose a saved stage name, one of five colors, and exactly three hats; the controller can move/jump the preview fighter. Join advances through depth, left/right chooses a spawn spotlight, and jump/note confirms entry with ground-colliding sparks.
- Production art pass: adapted the supplied backstage image, generated and keyed a transparent lobby apron and three-hat strip, and generated keyed left/right curtains plus valance for the reveal. Chroma working files live in ignored `encore-royale/art-source/`.
- Inspected the user's Unity `BulletAge-WorkingCopy` as a read-only design reference. Reusable ideas for later web-native adaptations: explicit enter/tick/exit states, data-driven movement ranges, one-shot camera impulses, hit time dilation, pooled expanding/fading dust, and centralized respawn reset. No Unity runtime, packages, or art were copied.
- Build and unit verification pass. Browser QA pass covers the installed-only gate, lobby art/customization, controller, spawn selection, entry, combat controls, pause/resume, parent curtain reveal, and an embedded iframe run; no browser warnings/errors were reported. The required headless harness was attempted twice but stalled at the local Chromium layer, so the in-app browser was used for the complete visual/interaction pass.
- Released the narrowly scoped game integration to GitHub Pages in commit `c732665`; the Pages build completed successfully and the live parent, launcher, compiled game, art, curtain, and service-worker assets all returned HTTP 200. Unrelated dirty working-tree changes remain unstaged.
- TODO after first live playtest: replace local bot simulation with an authoritative realtime room server and short-lived signed access token; tune movement/combat from phone feedback; add more hats/spawn maps without changing the initial three-option lobby.
- Portrait playtest pass: fixed the secret songbook pull gate (`genreFilter` uses `all`, not the visible label), widened the bottom tolerance, and lets one continuous touch gesture become the resisted pull as it reaches the end. Service-worker cache advanced to v31.
- Rebuilt the portrait composition so customization occupies the upper negative space, the blurred live stage remains unobstructed, the enlarged fully opaque preview fighter lives on a lit foreground apron with a following nameplate, and the installed controller owns the lower region.
- Gameplay readability pass: two bots by default, 1.48x fighter rendering, a 1.72x player-following arena camera after entry, ornate burgundy/gold draped platforms, removed opaque purple edge wedges, local fighter contrast, culled song labels, cleaner musical projectile trails, and a smaller player-anchored MIC DROP callout.
- Controller captions now read DODGE, JUMP, and NOTE. Added optional device haptics for player jump, fire, successful impact, death, and respawn; unsupported browsers ignore them safely.
- Unit tests and production TypeScript/Vite build pass. Portrait browser QA passed for lobby and joined gameplay with no browser warnings/errors; the long natural catalog-scroll exercise reached deep into the virtualized list before the automation deadline, while the corrected eligibility values and touch transition were verified in code.
- Logo/PWA follow-up: replaced legacy key marks with the supplied gold key-triangle logo and added the matching question-mark mark; wired logo use through the header, landing door, membership/glyph surfaces, info modal, chat branding, and signed-in state. Added the Break the Ice chat prompts, admin-session restoration/menu refresh updates, cache-busted all modified entrypoints, and expanded the installed-app shell cache to v32 for the new assets.
- Mobile venue/PWA repair: restored natural wrapping in the What is BCD copy at portrait widths, removed the competing two-column scene below 520px, and added an installed-app Refresh/Update control that checks the service worker without HTTP cache reuse before reopening the newest page. App shell cache advanced to v33.
- QA: verified the repaired venue modal at a 390×844 portrait viewport; story, address, hours, and both actions fit without horizontal clipping and browser warnings/errors were empty. The required game harness also reloaded the installed-only gate successfully after the PWA changes.
- Question-logo hotfix: the actual clickable venue-info button now renders the supplied gold question-triangle asset; removed the ineffective duplicate `brandrow` pseudo-element and advanced the installed-app shell to cache v34.
- Celestefall replacement: preserved the rejected Encore mobile-controls iteration on local branch `archive/encore-controls-20260830`; forked the MIT-licensed upstream project to `ijustcreate/Celestefall`; added and tested a responsive HTML Canvas port in that fork; and changed the installed-only curtain/admin launcher to load the independently deployed Celestefall GitHub Pages build. The old Encore build remains in the repository but is no longer loaded or pre-cached.
- Celestefall three-screen update: bumped the isolated game URL and installed-app cache after the fork gained a one-display-tall, three-screen-wide traversable level, centered portrait camera overscan, smoother TowerFall-inspired motion tuning, crouch/look-up poses, jump buffering, coyote time, variable jump height, and a verified full-map route.
- Bullet Age Ash integration: advanced the isolated game URL and installed-app shell after the web fork replaced the pirate with the user's original Ash Spine rig. The browser build now uses Ash's authored idle, run, jump, fall, crouch, aim-up, and wall-cling animations without Unity, while retaining a load-failure fallback.
- Celestefall combat update: bumped the isolated game URL and installed-app service-worker cache after adding automatic airborne wall cling/wall jump, musical-note Shoot with hit pushback, directional Dash, and one blue Player2 Bullet Age sparring bot with infinite respawns. The game fork also corrects touch-landscape aspect ratio/recentering and uses a three-button Shoot/Dash/Jump cluster.
- Ash Skin Studio integration: the installed BCD launcher now passes a narrow `isAdmin` flag in its existing postMessage handshake. Authorized admins get a top-right Skin Studio inside Celestefall for creating, previewing, saving, applying, and deleting device-local Ash palette/equipment variants; non-admin sessions never render the editor control. Advanced the game build query and installed-app shell cache to v48.
- Celestefall control-layout correction: advanced the iframe build query and launcher cache key, then bumped the installed-app shell to v49 so home-screen installs fetch the portrait/landscape control fix and bottom-anchored panoramic ground crop.
